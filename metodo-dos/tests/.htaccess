# Bloquea acceso web directo a este directorio (Apache 2.4 y 2.2).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
